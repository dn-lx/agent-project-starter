# Claude Code bootstrap

`AGENTS.md` is the canonical project instruction source.

At session start:
1. read `AGENTS.md`,
2. read `docs/PROJECT-MEMORY.md`,
3. read `docs/CURRENT-HANDOFF.md`,
4. read `docs/AGENT-PLATFORM-WORKFLOWS.md`,
5. read the relevant `.agents/skills/` instructions,
6. verify required MCP servers before relying on them.

Do not duplicate or override universal project rules here. Durable decisions belong in repository documentation, not Claude session history.
