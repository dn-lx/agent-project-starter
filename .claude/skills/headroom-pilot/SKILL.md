---
name: headroom-pilot
description: Pilot Headroom context compression only when measured agent context/tool output is a real bottleneck, and compare quality before wider adoption.
---

<!-- Generated by scripts/sync-claude-skills.mjs; edit the canonical skill. -->

Read and follow `.agents/skills/headroom-pilot/SKILL.md` from the repository root before doing this task. That file is the canonical procedure; this adapter only enables Claude discovery.

Resolve relative references, scripts and assets from `.agents/skills/headroom-pilot/`, not this adapter directory. Follow root `AGENTS.md` for branch, versioning, permissions and handoff rules.
