---
name: motion-runtime
description: Implement purposeful production UI animation with Motion when CSS transitions are insufficient, while preserving accessibility, performance and the project's existing stack.
---

<!-- Generated by scripts/sync-claude-skills.mjs; edit the canonical skill. -->

Read and follow `.agents/skills/motion-runtime/SKILL.md` from the repository root before doing this task. That file is the canonical procedure; this adapter only enables Claude discovery.

Resolve relative references, scripts and assets from `.agents/skills/motion-runtime/`, not this adapter directory. Follow root `AGENTS.md` for branch, versioning, permissions and handoff rules.
