---
name: code-hygiene
description: Find and remove confirmed dead code, unused exports/dependencies and obsolete paths without breaking dynamic/runtime behavior.
---

<!-- Generated by scripts/sync-claude-skills.mjs; edit the canonical skill. -->

Read and follow `.agents/skills/code-hygiene/SKILL.md` from the repository root before doing this task. That file is the canonical procedure; this adapter only enables Claude discovery.

Resolve relative references, scripts and assets from `.agents/skills/code-hygiene/`, not this adapter directory. Follow root `AGENTS.md` for branch, versioning, permissions and handoff rules.
